import { Box, Button } from '@mui/joy';
import ChipDelete from '@mui/joy/ChipDelete';

export default function DeletePersonality({id} : {id:number}){
    return (
        <button >
            test
        </button>
    )
}